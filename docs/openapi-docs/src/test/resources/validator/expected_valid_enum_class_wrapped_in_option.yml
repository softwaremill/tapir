openapi: 3.0.3
info:
  title: Entities
  version: '1.0'
paths:
  /:
    get:
      operationId: getRoot
      parameters:
        - name: color
          in: query
          required: false
          schema:
            type: string
            enum:
              - blue
              - red
      responses:
        '200': {}
        '400':
          description: 'Invalid value for: query parameter color'
          content:
            text/plain:
              schema:
                type: string
