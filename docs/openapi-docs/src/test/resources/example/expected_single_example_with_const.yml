openapi: 3.1.0
info:
  title: Entities
  version: '1.0'
paths:
  /:
    get:
      operationId: getRoot
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FruitConst'
components:
  schemas:
    FruitConst:
      title: FruitConst
      type: object
      required:
        - fruitType
      properties:
        fruitType:
          type: string
          const: Golden Delicious
