openapi: 3.1.0
info:
  title: Examples
  version: '1.0'
paths:
  /:
    post:
      operationId: postRoot
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContainsList'
        required: true
      responses:
        '200':
          description: ''
        '400':
          description: 'Invalid value for: body'
          content:
            text/plain:
              schema:
                type: string
components:
  schemas:
    ContainsList:
      title: ContainsList
      type: object
      properties:
        l:
          type: array
          items:
            type: integer
            format: int32
          example:
            - 1
            - 2
            - 3