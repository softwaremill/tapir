openapi: 3.1.0
info:
  title: Merge part 1
  version: '1.0'
paths:
  /merge/part1:
    get:
      operationId: mergePart1GetAlpha
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Alpha'
components:
  schemas:
    Alpha:
      type: object
      required:
        - value
      properties:
        value:
          type: string
